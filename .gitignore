# dependencies
node_modules
.pnp
.pnp.js

# next
.next
out
build

# misc
.DS_Store
*.log
npm-debug.log*

# env
.env
.env.local
.env*.local

# runtime data (local JSON store — schema SQL lives in database/ and IS tracked)
database/store.json

# Bulk Email — uploaded batch PDFs live on the server's disk, never in git
uploads/

# editor
.vscode
.idea
